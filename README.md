# FileEncryptor
C# File Encryptor
IMPORTANT!: It does not encrypt exes but other files can be encrypted. So for exe use something like sending it over HTTP with a custom encryption method built from scratch, if you use a prebuilt encryption standard method it might be cracked.
